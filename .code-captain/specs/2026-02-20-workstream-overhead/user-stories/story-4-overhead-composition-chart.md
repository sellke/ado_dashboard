---
# Story 4: Overhead Composition Chart Component

> **Status:** Completed ✅
> **Priority:** Medium
> **Dependencies:** Story 3 (types must be defined)

## User Story

**As a** stakeholder reviewing a workstream,
**I want** a stacked bar chart showing how ceremony, bug, spike, and support hours contribute to overhead per sprint,
**So that** I can see at a glance which overhead categories are growing and how the composition changes over time.

## Acceptance Criteria

- [x] Given `overheadComposition` data with 5 sprints, when the chart renders, then 5 grouped stacked bars appear with 4 layers (ceremony, bugs, spikes, support) ✅
- [x] Given a sprint where `bugHours` is `0`, when the chart renders, then the bug layer is absent or zero-height for that sprint ✅
- [x] Given an empty `composition` array, when the component renders, then nothing is rendered (null return) ✅
- [x] Given the chart, when it renders, then it uses a compact height (~200px) consistent with the velocity trend chart ✅
- [x] Given the chart, when it renders, then each series has a distinct color and a legend is shown ✅

## Implementation Tasks

- [x] 4.1 Write unit tests for `OverheadCompositionChart` (renders chart, handles empty array, renders correct series) ✅
- [x] 4.2 Create `components/Dashboard/OverheadCompositionChart.tsx` using Mantine `BarChart` with `type="stacked"` ✅
- [x] 4.3 Define four series: `{ name: 'Ceremony', color: 'blue.6' }`, `{ name: 'Bugs', color: 'red.6' }`, `{ name: 'Spikes', color: 'orange.6' }`, `{ name: 'Support', color: 'yellow.6' }` ✅
- [x] 4.4 Map `OverheadCompositionViewModel[]` to `BarChart` data format: `{ sprint: sprintName, Ceremony: ceremonyHours, Bugs: bugHours, Spikes: spikeHours, Support: supportHours }` ✅
- [x] 4.5 Set chart height to 200, add `withLegend` prop, use abbreviated sprint names on X-axis ✅
- [x] 4.6 Handle empty state: return `null` when `composition.length === 0` ✅

## Notes

- Uses Mantine `BarChart` from `@mantine/charts` — the same package used by `VelocityTrendChart`
- `type="stacked"` is the Mantine BarChart prop for stacked mode
- `OverheadCompositionViewModel` hour fields are already `number` (null → 0 was handled in adapter), so no null checks needed in the chart component
- Keep the component pure/presentational — no data fetching, just props
- Color choices are suggestions; match the existing Mantine color token style used in `VelocityTrendChart`

## Definition of Done

- [x] All tasks completed ✅
- [x] All acceptance criteria met ✅
- [x] Tests passing (10/10, 100%) ✅
- [x] Code reviewed ✅
- [x] Chart renders correctly with real data ✅
