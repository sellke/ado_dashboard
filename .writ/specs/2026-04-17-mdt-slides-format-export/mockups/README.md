# Mockups & Visual References

Stakeholders chose **screenshots** as the visual reference channel for this spec.

## Primary reference (on file)

| File | Role | Used by |
| --- | --- | --- |
| [`mdt-template-slide-reference.png`](./mdt-template-slide-reference.png) | Full **LAYOUT_WIDE** still (1280×720): title, optional subtitle, blank body, footer + wordmark | Story 2 (frame/footer), manual QA for all stories |

## How to add more screenshots

1. Export a **reference** Medtronic template slide or an example `.pptx` frame that product accepts as gold standard (if permitted).
2. Save PNG or PDF exports under this folder with descriptive names (e.g. `table-reference.png`).
3. Add a row to the table above and a short note in `../spec.md` → Visual references if the spec should cite it.

## Normative source

The authoritative rules remain in **`.cursor/skills/mdt_slides.md`**. Screenshots here are **evidence** for verification, not a replacement for the skill.

## Privacy

Do not commit customer-confidential content; use redacted or internal sample decks only.
